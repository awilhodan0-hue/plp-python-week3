# PLP Python Week 3

`grade_reporter.py` - Calculates grades, pass/fail counts, and the average score.

`bug_hunt.py` - Fixes three bugs in a program that calculates the sum of 1 to 5.

The hardest bug to find was the loop condition because the program ran without an error message. I knew something was wrong because the answer was not the required 15, which showed that the program was not including all the numbers.
